"""Shared core utilities.

- ``config`` — environment-driven settings (``ASTROCYTE_*``).
- ``policy`` — the actuation safety policy engine (ADR-014).
- ``connector`` — the DataConnector contract (seeds #34).
- ``llm`` — model routing across nodes (ADR-013).

Logging and further cross-cutting helpers land with the Ops Agent core (#7).
"""
