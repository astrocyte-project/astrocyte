/**
 * Placeholder application shell. The real app-management dashboard is built in
 * #14 (React 19 + Vite + FastAPI). This exists so the web CI job (lint, type
 * check, test, build) has a real target.
 */
export function App() {
  return (
    <main>
      <h1>Astrocyte</h1>
      <p>Self-Hosted AI Operating System — dashboard coming soon.</p>
    </main>
  );
}
